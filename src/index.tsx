import * as React from "react";
import * as ReactDOM from "react-dom";
import "./styles.scss"

const ROOT = document.querySelector(".container");

ReactDOM.render(<h1 className={'test'}>Hello</h1>, ROOT);